# Outputs

Store generated artifacts here so your submission is reproducible.

- `output/charts/` — exported plots (PNG/HTML)
- `output/tables/` — derived tables (CSV/Parquet)
- `output/reports/` — short write-ups (Markdown/PDF)

Tip: Plotly charts can be saved as interactive HTML without extra dependencies.
